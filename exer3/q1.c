/**
 * Create a struct named "Student" with these fields:
 * - name: a string not more than 100 characters
 * - idNumber: an integer
 * - college: a string exactly 3 characters
 * - course: a string exactly 5 characters
 * - courseSpeciality: a string exactly 5 characters
 *
 * Create a program that would ask those fields, create a struct from said fields,
 * and then print all fields.
 *
 * For college, map the 3 character code to our colleges here (eg: CCS == College of Computer Studies)
 */